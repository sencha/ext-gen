Ext.define('{appName}.view.users.UsersViewModel', {
	extend: 'Ext.app.ViewModel',
	alias: 'viewmodel.usersviewmodel',
	data: {
		name: '{appName}'
	},
	stores: {
		users: {
			type: 'users'
		}
	}
});
