Ext.define('{appName}.view.main.MainViewController', {
  extend: 'Ext.app.ViewController',
  alias: 'controller.mainviewcontroller',

  onButtonClick: function (button) {
    var vm = this.getViewModel()
    vm.set('clickTime', Date.now())
  }

})
